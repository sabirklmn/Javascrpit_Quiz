git status = using current 

git status or git branch = checking branch name

git init= create git directory

ls -all= hidden directory files

git add filename  picking

git add .  =all files picking


git commit -m " message pass files refer"

git log = all commit messages in see

git checkout commitid= previous file head move // detached head

    return main head command
    git checkout master

git diff = difference modified code +++signs
     git diff = two commit id use differce use

     git diff HEAD~1 HEAD use

     git diff HEAD~1 HEAD --name-only   
     using files only display

git restore =previous file headmove but all deletes after sections 
    !!danger





